## What does this PR do?

<!-- Short description of the change and why it's needed. -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run build` passes
- [ ] Schema changes include a migration (`npx prisma migrate dev`)
- [ ] New integrations follow the driver pattern ([docs/extensions.md](../docs/extensions.md))
- [ ] UI strings use `getT()` where the page is already localized
